{
  "schemaVersion": 1,
  "jsonTargetSchemaVersion": 2,
  "globalJsonEnvelope": ["schemaVersion", "command", "status", "repoRoot", "data", "error"],
  "rootParserOptions": [
    "--prompt",
    "--fzf-arg",
    "--json",
    "--verbose",
    "--hooks",
    "--no-hooks",
    "--gh",
    "--no-gh",
    "--full-path",
    "--allow-unsafe",
    "--strict-post-hooks",
    "--hook-timeout-ms",
    "--lock-timeout-ms",
    "--allow-agent",
    "--allow-shared",
    "--reason",
    "--owner",
    "--force",
    "--force-dirty",
    "--allow-unpushed",
    "--force-unmerged",
    "--force-locked",
    "--apply",
    "--dry-run",
    "--current",
    "--from",
    "--to",
    "--stash",
    "--keep-stash",
    "--fallback",
    "--no-fallback",
    "--install",
    "--path",
    "--help",
    "--version"
  ],
  "commands": [
    {
      "name": "init",
      "repoLock": true,
      "hooks": ["pre-init", "post-init"],
      "tty": "none",
      "options": ["--json", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "empty or JSON", "effects": ["create managed root", "create metadata directories", "update git exclude idempotently"], "evidence": "init creates required directories and keeps exclude idempotent" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["alreadyInitialized"]
    },
    {
      "name": "list",
      "repoLock": false,
      "hooks": [],
      "tty": "ANSI colors only when interactive; width-aware path truncation",
      "options": ["--json", "--no-gh", "--full-path", "--monitor"],
      "normal": { "exitCode": 0, "stdout": "colorized table or JSON", "effects": ["atomically merge lifecycle observations", "monitor mode skips upstream probes and lifecycle observation writes"], "evidence": "list applies catppuccin colors in interactive mode; monitor mode preserves schema v2 with unknown upstream state" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["baseBranch", "managedWorktreeRoot", "worktrees"]
    },
    {
      "name": "status",
      "repoLock": false,
      "hooks": [],
      "tty": "none",
      "options": ["--json"],
      "normal": { "exitCode": 0, "stdout": "four status lines or JSON", "effects": ["atomically merge lifecycle observations"], "evidence": "status --json without branch shows current worktree" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["worktree"]
    },
    {
      "name": "path",
      "repoLock": false,
      "hooks": [],
      "tty": "none",
      "options": ["--json"],
      "normal": { "exitCode": 0, "stdout": "absolute path or JSON", "effects": ["atomically merge lifecycle observations"], "evidence": "path --json returns absolute worktree path" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["branch", "path"]
    },
    {
      "name": "switch",
      "repoLock": true,
      "hooks": ["pre-switch", "post-switch"],
      "tty": "none",
      "options": ["--json", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "absolute path or JSON", "effects": ["reuse or create worktree", "write lifecycle metadata"], "evidence": "switch creates and then reuses worktree path" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["branch", "path", "disposition"]
    },
    {
      "name": "new",
      "repoLock": true,
      "hooks": ["pre-new", "post-new"],
      "tty": "none",
      "options": ["--json", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "absolute path or JSON", "effects": ["create branch", "create worktree", "write lifecycle metadata"], "evidence": "new without branch creates wip branch" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["branch", "path"]
    },
    {
      "name": "mv",
      "repoLock": true,
      "hooks": ["pre-mv", "post-mv"],
      "tty": "none",
      "options": ["--json", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "new absolute path or JSON", "effects": ["rename branch", "move worktree", "move lifecycle metadata", "move lock metadata when present"], "evidence": "mv renames current worktree branch and path" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["branch", "path"]
    },
    {
      "name": "del",
      "repoLock": true,
      "hooks": ["pre-del", "post-del"],
      "tty": "force flags require explicit unsafe authorization in non-TTY mode",
      "options": ["--json", "--force", "--force-dirty", "--allow-unpushed", "--force-unmerged", "--force-locked", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "deleted path or JSON", "effects": ["remove worktree", "delete branch", "delete lock and lifecycle metadata"], "evidence": "del rejects unmerged by default and succeeds with force" },
      "failure": { "exitCode": 4, "code": "UNMERGED_WORKTREE", "evidence": "del rejects unmerged by default and succeeds with force" },
      "jsonDataFields": ["branch", "path"]
    },
    {
      "name": "gone",
      "repoLock": true,
      "hooks": ["pre-gone", "post-gone"],
      "tty": "none",
      "options": ["--json", "--dry-run", "--apply"],
      "normal": { "exitCode": 0, "stdout": "candidate or deleted lines, or JSON", "effects": ["dry-run has no mutation", "apply removes eligible worktrees and metadata"], "evidence": "gone dry-run then apply removes overall-merged candidates without upstream tracking" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["dryRun", "candidates", "deleted", "failed"]
    },
    {
      "name": "adopt",
      "repoLock": true,
      "hooks": ["pre-adopt", "post-adopt"],
      "tty": "none",
      "options": ["--json", "--dry-run", "--apply"],
      "normal": { "exitCode": 0, "stdout": "candidate, moved, skipped, or failed lines, or JSON", "effects": ["dry-run has no mutation", "apply moves unmanaged worktrees under managed root"], "evidence": "adopt dry-run then apply moves unmanaged worktrees into managed root" },
      "failure": { "exitCode": 4, "code": "SAFETY_REJECTED", "evidence": "adopt --apply --json returns error status when move failures are present" },
      "jsonDataFields": ["dryRun", "managedWorktreeRoot", "candidates", "moved", "skipped", "failed"]
    },
    {
      "name": "get",
      "repoLock": true,
      "hooks": ["pre-get", "post-get"],
      "tty": "none",
      "options": ["--json", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "absolute path or JSON", "effects": ["fetch remote", "create tracking branch when absent", "create worktree and lifecycle metadata"], "evidence": "get creates tracked local branch worktree from remote branch" },
      "failure": { "exitCode": 3, "code": "INVALID_REMOTE_BRANCH_FORMAT", "evidence": "returns INVALID_REMOTE_BRANCH_FORMAT for malformed get target" },
      "jsonDataFields": ["branch", "path", "disposition"]
    },
    {
      "name": "extract",
      "repoLock": true,
      "hooks": ["pre-extract", "post-extract"],
      "tty": "none",
      "options": ["--json", "--current", "--stash", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "new absolute path or JSON", "effects": ["move primary branch to managed worktree", "checkout base in primary", "optionally transfer stash"], "evidence": "extract moves current primary branch into .worktree" },
      "failure": { "exitCode": 3, "code": "INVALID_ARGUMENT", "evidence": "returns INVALID_ARGUMENT when extract uses --current and --from together" },
      "jsonDataFields": ["branch", "path"]
    },
    {
      "name": "absorb",
      "repoLock": true,
      "hooks": ["pre-absorb", "post-absorb"],
      "tty": "non-TTY requires --allow-agent and --allow-unsafe",
      "options": ["--json", "--from", "--keep-stash", "--allow-agent", "--no-hooks", "--allow-unsafe"],
      "normal": { "exitCode": 0, "stdout": "primary path or JSON", "effects": ["stash source changes", "merge or fast-forward into primary", "apply and optionally retain stash"], "evidence": "absorb applies non-primary worktree changes into primary" },
      "failure": { "exitCode": 10, "code": "HOOK_FAILED", "evidence": "absorb auto-restores source changes when pre-hook fails" },
      "jsonDataFields": ["branch", "path", "sourcePath", "stashed", "stashRef"]
    },
    {
      "name": "unabsorb",
      "repoLock": true,
      "hooks": ["pre-unabsorb", "post-unabsorb"],
      "tty": "non-TTY requires --allow-agent and --allow-unsafe",
      "options": ["--json", "--to", "--keep-stash", "--allow-agent", "--allow-unsafe", "--no-hooks"],
      "normal": { "exitCode": 0, "stdout": "target path or JSON", "effects": ["stash primary changes", "merge or fast-forward into target", "apply and optionally retain stash"], "evidence": "unabsorb applies primary changes into non-primary worktree" },
      "failure": { "exitCode": 10, "code": "HOOK_FAILED", "evidence": "unabsorb auto-restores primary changes when pre-hook fails" },
      "jsonDataFields": ["branch", "path", "sourcePath", "stashed", "stashRef"]
    },
    {
      "name": "use",
      "repoLock": true,
      "hooks": ["pre-use", "post-use"],
      "tty": "non-TTY requires --allow-agent and --allow-unsafe; shared branch requires --allow-shared",
      "options": ["--json", "--allow-agent", "--allow-shared", "--allow-unsafe", "--no-hooks"],
      "normal": { "exitCode": 0, "stdout": "primary path or JSON", "effects": ["checkout target branch in primary worktree"], "evidence": "use requires explicit non-TTY allow flags and then checks out branch" },
      "failure": { "exitCode": 4, "code": "UNSAFE_FLAG_REQUIRED", "evidence": "use requires explicit non-TTY allow flags and then checks out branch" },
      "jsonDataFields": ["branch", "path"]
    },
    {
      "name": "exec",
      "repoLock": false,
      "hooks": [],
      "tty": "human mode inherits child stdin, stdout, and stderr; JSON mode captures child stdout and stderr",
      "options": ["--json", "--"],
      "normal": { "exitCode": 0, "stdout": "child-owned stream in human mode or one JSON object with captured streams", "effects": ["run child process in selected worktree"], "evidence": "exec validates arguments after -- and supports --json success payload" },
      "failure": { "exitCode": 21, "code": "CHILD_PROCESS_FAILED", "evidence": "exec returns child failure as 21" },
      "jsonDataFields": ["branch", "path", "childExitCode", "childStdout", "childStderr"]
    },
    {
      "name": "invoke",
      "repoLock": false,
      "hooks": [],
      "tty": "hook stdio follows hook runner contract",
      "options": ["--json", "--"],
      "normal": { "exitCode": 0, "stdout": "empty or JSON", "effects": ["execute named hook with arguments and hook environment"], "evidence": "invoke executes existing hook" },
      "failure": { "exitCode": 3, "code": "INVALID_ARGUMENT", "evidence": "returns INVALID_ARGUMENT for malformed invoke hook name" },
      "jsonDataFields": ["hook"]
    },
    {
      "name": "copy",
      "repoLock": false,
      "hooks": [],
      "tty": "none",
      "options": ["--json"],
      "normal": { "exitCode": 0, "stdout": "empty or JSON", "effects": ["copy repo-root paths into current target worktree"], "evidence": "copy and link helper commands place files into WT_WORKTREE_PATH" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["copied", "worktreePath"]
    },
    {
      "name": "link",
      "repoLock": false,
      "hooks": [],
      "tty": "none",
      "options": ["--json", "--no-fallback"],
      "normal": { "exitCode": 0, "stdout": "empty or JSON", "effects": ["create relative symlinks into current target worktree"], "evidence": "copy and link helper commands place files into WT_WORKTREE_PATH" },
      "failure": { "exitCode": 2, "code": "NOT_GIT_REPOSITORY", "evidence": "returns a JSON NOT_GIT_REPOSITORY failure for every repository command" },
      "jsonDataFields": ["linked", "worktreePath"]
    },
    {
      "name": "lock",
      "repoLock": true,
      "hooks": [],
      "tty": "none",
      "options": ["--json", "--owner", "--reason"],
      "normal": { "exitCode": 0, "stdout": "empty or JSON", "effects": ["create or update lock metadata"], "evidence": "lock and unlock update lock state" },
      "failure": { "exitCode": 4, "code": "LOCK_CONFLICT", "evidence": "handles lock/unlock conflicts for invalid metadata and owner mismatch" },
      "jsonDataFields": ["branch", "reason", "owner"]
    },
    {
      "name": "unlock",
      "repoLock": true,
      "hooks": [],
      "tty": "none",
      "options": ["--json", "--owner", "--force"],
      "normal": { "exitCode": 0, "stdout": "empty or JSON", "effects": ["remove lock metadata idempotently"], "evidence": "lock and unlock update lock state" },
      "failure": { "exitCode": 4, "code": "LOCK_CONFLICT", "evidence": "handles lock/unlock conflicts for invalid metadata and owner mismatch" },
      "jsonDataFields": ["branch"]
    },
    {
      "name": "cd",
      "repoLock": false,
      "hooks": [],
      "tty": "requires an interactive selector surface; stdout may be piped when stderr is a TTY; cancellation exits 130",
      "options": ["--json", "--prompt", "--fzf-arg"],
      "normal": { "exitCode": 0, "stdout": "selected absolute path or JSON", "effects": ["invoke fzf with tab-separated candidates and preview payload", "atomically merge lifecycle observations"], "evidence": "cd shows home-relative path in picker but returns absolute path" },
      "failure": { "exitCode": 5, "code": "DEPENDENCY_MISSING", "evidence": "cd maps dependency errors and parses prompt/fzf args" },
      "jsonDataFields": ["path"]
    },
    {
      "name": "completion",
      "repoLock": false,
      "hooks": [],
      "tty": "none",
      "options": ["--json", "--install", "--path"],
      "normal": { "exitCode": 0, "stdout": "completion script, install message, or JSON", "effects": ["optional atomic installation of completion file"], "evidence": "prints zsh completion script outside git repository" },
      "failure": { "exitCode": 3, "code": "INVALID_ARGUMENT", "evidence": "returns a JSON INVALID_ARGUMENT failure for an unsupported completion shell" },
      "jsonDataFields": ["shell", "installed", "path", "script"]
    }
  ]
}
